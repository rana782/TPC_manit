# Optional: loaded before backend/.env (see backend/src/loadEnv.ts). Prefer backend/.env for app config.
PORT=5001
# Supabase session pooler URI (or local Postgres)
DATABASE_URL="postgresql://postgres.your-project-ref:your-url-encoded-password@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require"
JWT_SECRET="supersecretjwtkey_change_in_production"
WEBHOOK_ENABLE="false"

# Frontend: use college-placement-portal/frontend/.env (VITE_*), not this file.
